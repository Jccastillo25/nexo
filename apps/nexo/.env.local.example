NEXT_PUBLIC_SUPABASE_URL=https://yrbjlmiqhkyxtlcerowh.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_3hLYjOkXzwquHu6JTHND0A_S8ViHAAJ

# Service role secret (Supabase -> Settings -> API). Solo se usa server-side
# (ver src/lib/supabase/admin.ts) para subir el logo/imagen de fondo del
# login a Storage (bucket "platform-assets") desde /ajustes. NUNCA exponer
# con NEXT_PUBLIC_, nunca commitear el valor real.
SUPABASE_SERVICE_ROLE_KEY=

# id de 'materiales-jcastillo' en core.companies (single-tenant por ahora,
# ver src/lib/company.ts) — mismo valor que apps/crm/.env.local.example.
NEXO_COMPANY_ID=2b8525bd-a01a-4b55-a32c-ffe70761d96d

# URL del deploy real de la zona /crm (Multi-Zones). En local, el puerto
# donde corras `pnpm --filter materiales-jcastillo-crm dev` (recomendado:
# 3001, para no chocar con el 3000 de esta app). En produccion, la URL del
# proyecto Vercel de apps/crm.
CRM_APP_URL=http://localhost:3001
