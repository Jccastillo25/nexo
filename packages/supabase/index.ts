// Clients tipados de Supabase, uno por schema del proyecto `nexo-core`.
// Los tipos se generaran con `supabase gen types typescript` una vez
// provisionado el proyecto (ver docs/DATABASE.md).
export {};
